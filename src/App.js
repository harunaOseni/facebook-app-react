import React from "react";
import Header from "./Header";
import "./App.css";

function App() {
  return (
    <div className="app">
      {/* Components */}

      {/* Header (at the top)*/}
      <Header />

      {/* App Body */}

      {/* SideBar (at the left side)*/}
      {/* Feed (In the middle)*/}
      {/* Widgets (at the right side ) */}

      {/* App Body */}

      {/* Components */}
    </div>
  );
}

export default App;
